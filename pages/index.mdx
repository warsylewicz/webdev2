# Welcome to CPRG 306 Web Development 2

<CurrentWeek />

export const CurrentWeek = () => {
    let newDate = new Date()
    let date = newDate.getDate();
    let month = newDate.getMonth() + 1;
    let year = newDate.getFullYear();

    const weeks = [{date: '2023-09-04', week: '1'}, { date: '2023-09-11', week: '2'}];

    return (
        <div>
            {year}-{month < 10 ? '0' + month : month }-{date}
            <br>
            </br>
            {weeks.map((week) => (
                <div key={week.date}>
                    {week.date} - {week.week}
                </div>
            ))}
        </div>
    );

};
