# Another Page

```js filename="demo.js" {3} copy
let a = 2;

console.log(a);
```

## Another external one

import Slideshow from "/components/Slideshow";

<Slideshow file="/module1/slides" />

## Sandpack!

import { Sandpack } from "@codesandbox/sandpack-react";

    <Sandpack
      template="react"
      customSetup={{
        dependencies: {
          "react-markdown": "latest",
        },
      }}
      files={{
        "/App.js": `import ReactMarkdown from 'react-markdown'

export default function App() {
  return (
    <ReactMarkdown>
      # Hello, *world*!
    </ReactMarkdown>
  )
}`,
      }}
    />
