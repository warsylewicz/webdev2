# Various Samples

## Multiple choice demo

import MultipleChoice from "components/MultipleChoice";

<MultipleChoice question="" choices={["", "", "", ""]} answer="4" />

<MultipleChoice
  question="Are the answers 0 indexed or 1 indexed?"
  choices={["0", "1"]}
  answer="2"
/>

## Code demo

```js filename="demo.js" copy
export default function App() {
  return <strong>Hello, world!</strong>;
}
```

## Sandpack demo

import Sandbox from "components/Sandbox";
import { files } from "./samples-demo.js";

<Sandbox template="react" files={files} />
