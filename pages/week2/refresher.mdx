# HTML

## Multiple choice problem

import MultipleChoice from "components/MultipleChoice";

<MultipleChoice
  question="What is your favourite colour?"
  choices={["red", "blue", "green", "yellow"]}
  answer="2"
/>

<MultipleChoice
  question="`let a = 5` What is your favourite colour?"
  choices={["blue", "green", "yellow"]}
  answer="1"
/>

## Code markdown demo

```js filename="demo.js" copy
import ReactMarkdown from "react-markdown";

export default function App() {
  return <ReactMarkdown># Hello, *world*!</ReactMarkdown>;
}
```

## Sandpack!

import Sandbox from "components/Sandbox";
import { files } from "./demo1.js";

<Sandbox template="react" files={files} />

## Another external one
